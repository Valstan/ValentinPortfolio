// Классы задач — «чем могу быть полезен». Отдельный файл: витрина работ и витрина услуг
// меняются независимо друг от друга.
//
// Целостность `proof` проверяется на импорте модуля (внизу файла) — то есть при
// `next build`, то есть на PR-гейте.

import { workBySlug } from '@/content/works';

export type Service = {
  id: string;
  title: string;
  /**
   * Класс услуги в два-пять слов — для перечисления состава в прозе, которую нельзя
   * набрать списком: мета-описание `/uslugi/`.
   *
   * Отдельно от `title`, а не `title.toLowerCase()`, потому что заголовок карточки и
   * строка в описании честно разные: на карточке «Мобильные приложения без магазинов
   * приложений» — это язык заказчика, в описании «мобильные PWA с пуш-уведомлениями» —
   * то, как услугу ищут. Сводить их в одну формулировку значило бы ухудшить обе.
   * Требование к теме одно: без запятой, иначе перечисление распадётся на лишние пункты.
   */
  topic: string;
  /** Формулировка от задачи заказчика, а не от технологии. */
  need: string;
  body: string;
  /** slug'и работ из works.ts — доказательства класса задач. */
  proof: string[];
};

export const SERVICES: Service[] = [
  {
    id: 'sites',
    topic: 'сайты и порталы под ключ',
    title: 'Сайты и порталы под ключ',
    need: 'Нужен сайт учреждению, событию или компании — и чтобы им можно было управлять без программиста',
    body:
      // Перенос «с адресами один в один» здесь не заявляем: работа, на которой это было
      // сделано, снята с витрины по решению заказчика, а /uslugi/ обещает, что каждое
      // доказательство можно открыть. Утверждение живёт в задаче о переносе — с оговоркой,
      // что снаружи его не проверить.
      'Сайт с админкой, в которой контент правит сам заказчик, поиск по материалам, разметка под поисковики и ИИ-ассистентов.',
    proof: ['gonba', 'sabantuy-malmyzh', 'yarmarka-kazanskaya'],
  },
  {
    id: 'erp',
    topic: 'настольные учётные системы',
    title: 'Настольные учётные системы',
    need: 'Нужен учёт на производстве или складе, а интернет на местах нестабильный',
    body:
      'Программа на компьютерах предприятия работает офлайн с локальной базой и сама синхронизируется с сервером, когда связь есть. Печать документов с подписями, обновление у всех пользователей разом, отказоустойчивый сервер.',
    proof: ['matrica-rmz'],
  },
  {
    id: 'automation',
    topic: 'сервисы автоматизации',
    title: 'Сервисы автоматизации',
    need: 'Рутина съедает время: собрать, отобрать и опубликовать контент вручную',
    body:
      'Сервис мониторит источники, отбирает нужное (в том числе с помощью ИИ) и публикует по расписанию сразу в несколько каналов. Фоновые очереди, устойчивость к сбоям внешних сервисов.',
    proof: ['sarafan', 'sarafan-reklama'],
  },
  {
    id: 'pwa',
    topic: 'мобильные PWA с пуш-уведомлениями',
    title: 'Мобильные приложения без магазинов приложений',
    need: 'Нужно приложение у людей в телефоне, но не хочется проходить публикацию в сторах',
    body:
      'Устанавливаемое веб-приложение (PWA) с офлайн-режимом и пуш-уведомлениями — работает и на iPhone, и на Android. Обновление выкатывается сразу всем, без ожидания модерации.',
    proof: ['trener'],
  },
  {
    id: 'auth',
    topic: 'единый вход и 152-ФЗ',
    title: 'Вход, роли и 152-ФЗ',
    need: 'В системе персональные данные — нужно сделать правильно с первого дня',
    body:
      'Единый вход для нескольких сервисов, роли и права с первого дня, соответствие закону о персональных данных, включая детские: согласие, локализация в РФ, минимизация, план действий при инциденте.',
    proof: ['trener', 'sarafan'],
  },
  {
    id: 'infra',
    topic: 'вывод в прод на российских серверах',
    title: 'Вывод в прод и эксплуатация',
    need: 'Код есть, а до работающего сайта на своём домене руки не доходят',
    body:
      // ⛔ D-038: не писать, что конкретные проды соседствуют на одном сервере — это состав
      // жильцов, ровно то, что предохранитель запрещает публиковать. Компетенция названа,
      // раскладка — нет.
      'Сборка в CI, деплой по SSH, HTTPS с автопродлением, кириллические домены, экономная эксплуатация на недорогих серверах с жёсткими лимитами памяти, проверка после каждого деплоя.',
    proof: ['gonba', 'vmalmyzhe'],
  },
];

/**
 * Состав витрины услуг одной строкой: «сайты и порталы под ключ, настольные…».
 *
 * Собрано так же, как `TASK_TOPICS` в `tasks.ts` и `BRIEF_TOPICS` в `brief.ts`, и
 * по той же причине: список классов в мета-описании `/uslugi/` был набран руками и
 * не сверялся ни с чем — седьмой класс не попал бы в него молча, а описание обещает
 * поисковику и ассистенту полный состав.
 */
export const SERVICE_TOPICS: string = SERVICES.map((service) => service.topic).join(', ');

/**
 * Целостность проверяется на импорте модуля, то есть при `next build`, то есть
 * на PR-гейте. Тот же приём, что `assertBrief` в `brief.ts`.
 *
 * Зачем: страница `/uslugi/` обещает читателю «за каждым классом стоит уже
 * запущенная система, которую можно открыть и посмотреть». Доказательства
 * названы слагом, а `workBySlug` смотрит в `VISIBLE_WORKS` — стоит работе
 * скрыться (`hidden: true`) или уйти из каталога, как ссылка молча превращается
 * в пустоту: `service.proof.map(...)` возвращает `null`, пункт исчезает, ошибки
 * нет. Ровно так `rmz-site` с 04.09 по 14.09 оставался доказательством у двух
 * классов («Сайты и порталы под ключ», «Вывод в прод и эксплуатация») уже после
 * того, как работу скрыли. Пусть лучше не собирается сборка.
 */
(function assertServices() {
  const seen = new Set<string>();

  for (const service of SERVICES) {
    if (seen.has(service.id)) {
      throw new Error(`services: дублирующийся id класса «${service.id}»`);
    }
    seen.add(service.id);

    // Без темы класс выпадет из мета-описания `/uslugi/` молча — строка просто
    // станет короче на один пункт.
    if (!service.topic.trim()) {
      throw new Error(`services: у класса «${service.id}» пустая тема — он выпадет из перечисления в описании /uslugi/`);
    }
    if (service.topic.includes(',')) {
      throw new Error(
        `services: тема класса «${service.id}» содержит запятую — перечисление склеивается запятыми и распадётся на лишние пункты`,
      );
    }

    if (service.proof.length === 0) {
      throw new Error(
        `services: у класса «${service.id}» нет ни одного доказательства, а /uslugi/ обещает систему за каждым классом`,
      );
    }
    for (const slug of service.proof) {
      if (!workBySlug(slug)) {
        throw new Error(
          `services: класс «${service.id}» ссылается на работу «${slug}», которой нет на витрине (снята, скрыта или переименована)`,
        );
      }
    }
  }
})();
