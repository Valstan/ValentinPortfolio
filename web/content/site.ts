// Единый источник фактов о сайте и его владельце (#051: факты в одном месте — нет дрейфа между страницами).

/** Канонический хост — только punycode (G133/G134: юникод-IDN ломает сниппеты при шаринге). */
export const SITE_ORIGIN = 'https://xn--80adkmnnb2b.xn--80adkdyec4j.xn--p1ai';

/** Человекочитаемая форма домена — только для отображения, никогда в href. */
export const SITE_HOST_DISPLAY = 'валентин.вмалмыже.рф';

export const PERSON = {
  name: 'Валентин Савиных',
  jobTitle: 'Full-cycle разработчик',
  alternateName: '@valstan',
  areaServed: 'Кировская область и удалённо по России',
} as const;

/**
 * Публичные каналы связи. Сервера у сайта нет (static export) — формы отправлять некуда,
 * поэтому лид приходит только по прямому каналу.
 *
 * Состав подтверждён владельцем 2026-08-02; почта и телефон приведены к каноническим
 * реквизитам владельца (решение 2026-08-09, D-018). Пустое поле не рендерится и не попадает
 * ни в JSON-LD, ни в /facts.json — чтобы добавить телефон, достаточно заполнить `phone`.
 * Контакты выводятся текстом, а не картинкой: их должны цитировать поиск и ИИ-ассистенты.
 */
export const CONTACTS = {
  telegram: 'https://t.me/VAL_STAN',
  telegramLabel: '@VAL_STAN',
  email: 'valstan@valstan.ru',
  github: 'https://github.com/Valstan',
  githubLabel: 'github.com/Valstan',
  // В href — без пробелов и скобок (иначе часть телефонов не наберётся), в подписи — читаемо.
  phone: '+79229005910',
  phoneLabel: '+7 922 900-59-10',
} as const;

/**
 * Яндекс.Метрика. Счётчик заведён 2026-08-22 под мандат brain от 09.08 (решение
 * владельца D-017): у каждого публичного сайта портфеля свой счётчик, и в подвале —
 * видимый информер с числом посетителей. Кабинет открывают раз в месяц, подвал —
 * каждый раз.
 *
 * Адрес счётчика в кабинете — `валентин.вмалмыже.рф`. Punycode-форму Метрика
 * отдельным адресом не принимает («Неверный формат URL»), IDN она нормализует сама:
 * соседние счётчики этого же аккаунта стоят на кириллических доменах и данные
 * собирают. Поэтому одного адреса достаточно, несмотря на G136.
 */
export const METRIKA_COUNTER_ID = 111856907;

/**
 * Разметка информера взята из кабинета as-is (G237) — не собрана по памяти.
 * Хвост `_0_uniques` — это «Посетители»; по умолчанию кабинет отдаёт `_pageviews`,
 * а владелец просил именно число посетителей.
 *
 * ⚠️ Приёмка информера — ТОЛЬКО глазами по картинке и только на непустой статистике:
 * он отдаёт валидный PNG 88×31 с нулями на каждой из ступеней включения, поэтому
 * «картинка загрузилась» ничего не доказывает.
 *
 * ⚠️ И обратная ловушка: `informer.yandex.ru` проверяет `Referer`. С localhost он
 * отдаёт 403 (проверено 22.08.2026: без Referer — 200 image/png 1287 B, с
 * `http://127.0.0.1:8099/` — 403 text/html, с прод-доменом — снова 200). То есть на
 * локальном превью информер выглядит сломанным ВСЕГДА, и это не дефект вёрстки.
 * Смотреть его только на проде.
 */
export const METRIKA_INFORMER_SRC =
  `https://informer.yandex.ru/informer/${METRIKA_COUNTER_ID}/3_1_FFFFFFFF_EFEFEFFF_0_uniques`;

export const METRIKA_STAT_URL =
  `https://metrika.yandex.ru/stat/?id=${METRIKA_COUNTER_ID}&from=informer`;

/** Каталог сервисов Малмыжа — стандарт онбординга сервиса (директива brain 2026-07-26). */
export const SERVICES_CATALOG_URL = 'https://xn--b1ae3a1a.xn--80adkdyec4j.xn--p1ai/services';

export const FOOTER_SIGNATURE = 'Сделано вМалмыже.рф (с) Валентин Савиных, 2026';

/**
 * Картинка для соцсетей и превью в ответах ассистентов.
 *
 * Лежит статикой в `public/`, а не генерируется роутом `app/opengraph-image.tsx`:
 * при `output: 'export'` генератор запекается в файл БЕЗ расширения (`out/opengraph-image`),
 * и nginx отдаёт его как `application/octet-stream` — та же болезнь, что у `/llms.txt`
 * (грабля с потерей `content-type` при экспорте). Соцсети такую картинку молча
 * игнорируют. Файл с расширением `.png` эту зависимость от настройки сервера снимает.
 *
 * Пересобрать при смене имени или должности: временно вернуть `app/opengraph-image.tsx`
 * с `ImageResponse` (нужен `export const dynamic = 'force-static'`, а каждый div с
 * несколькими детьми — с явным `display: flex`), собрать и скопировать
 * `out/opengraph-image` в `public/og.png`.
 */
export const OG_IMAGE = {
  url: absoluteUrl('/og.png'),
  width: 1200,
  height: 630,
  alt: `${PERSON.name} — ${PERSON.jobTitle.toLowerCase()}, ${PERSON.areaServed}`,
} as const;

/**
 * Абсолютный URL в punycode-форме. Прогонять через него ВСЕ исходящие абсолютные ссылки:
 * `new URL()` каноникализирует хост в punycode и percent-кодирует юникод в пути.
 */
export function absoluteUrl(path = '/'): string {
  return new URL(path, SITE_ORIGIN).href;
}
