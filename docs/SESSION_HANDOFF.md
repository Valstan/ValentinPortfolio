# Портфолио (ValentinPortfolio) — Session Handoff

> Sticky-note для непрерывности сессий. Обновляется **по шагам нитки в том же PR, что и работа** (D-066); `/close_session` — страховка. История — `git log -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE — почта brain по 02.09 закрыта, мандатов в очереди нет, прод живой
**Updated:** 2026-09-02 (аудит инструкций под Fable 5.1, D-038/D-046/D-066, хук SessionStart)
**Branch:** main

## Где мы

Одним PR закрыты девять писем brain с 23.08: канон переписан «от цели» (прод есть, мерж кода = деплой, без дат-как-обоснований), предохранитель — через свойство (репо + сайт + логи Actions), правило D-046 «текст файлом», `/obriv` удалён, стоит хук `SessionStart`, `close_session` переведён на `-F`/`--body-file`. Ответ brain — `mailbox/to-brain/2026-09-02-instruction-audit-applied-valentin.md`. Точка возврата — тег `pre-fable-freeze-2026-09-02`.

## Что найдено по дороге

- **D-038-обход в августе пропустил технический VPS-домен хостера** в `PORTFOLIO.md` (прод чужого проекта): не IP, не «бокс», а домен третьего уровня, похожий на продуктовый URL. Категория «технические домены хостера» добавлена в предохранитель. На сайте адреса не было.
- **Контроль грепа обязателен:** первый «контрольный» шаблон («Джино» в AGENTS.md) дал пустоту не из-за слепоты инструмента, а потому что строку убрал PR #28 до pull. Контроль надо брать по строке, которая точно есть в **текущем** дереве, а не по памяти о файле.
- **Workflow'ы чистые по письмам 29.08/01.09:** `vars` не используются, дампов журнала нет; править нечего.

## Грабли Метрики (проверено, не по памяти)

- **Информер проверяет Referer.** С localhost — **403**, с прод-домена — 200. На локальном превью он сломан ВСЕГДА; это не дефект вёрстки. Смотреть только на проде.
- **Информер отстаёт от отчётов на часы** (G237). Код 200 ничего не доказывает; приёмка — по изменению размера PNG.
- **G136 для Метрики невыполним:** punycode-форму она не принимает, IDN нормализует сама. Одного адреса достаточно.
- Информер делает статистику сайта публичной (следствие D-017), об этом сказано на главной.

## Грабли прошлых сессий (помнить)

1. **Next 15.5.4 не собирает static export на Windows** — лечится обновлением до 15.5.22.
2. **При static export теряется `content-type` из route-handler**: `/llms.txt` без `charset` → кириллица в кашу. Лечится на стороне nginx.
3. **Смоук из GitHub Actions до российского хоста ненадёжен** — раннеры вне РФ. Смоук на боксе.
4. **Внешний таймаут ≠ «сайт лежит»** — сперва сверь, куда указывает домен.
5. **fullPage-скриншот не ждёт `loading="lazy"`** — прокрути страницу перед съёмкой.
6. **`out/` держится запущенным http-сервером** — `next build` падает с `EBUSY: rmdir`. Остановить сервер перед пересборкой.
7. **Сканер секретов не проверять примером из документации вендора** (`AKIAIOSFODNN7EXAMPLE` в allowlist gitleaks, G258) — только случайным токеном.
8. **Меняя поведение сайта, грепни сайт на утверждения об этом поведении** (Метрика и подпись к кадрам Матрицы дважды становились ложью).

## Следующий шаг

1. **Сайт РМЗ скрыт** по решению владельца — вернуть показ снятием флага `hidden: true` в `works.ts`, когда заказчик разрешит.
2. **Даты замеров стареют.** Числа Сарафана, коммиты Матрицы и Гоньбы — пересматривать не реже раза в квартал; ближайший срок — до конца октября 2026.
3. Держать каталог в синхроне с реестром Мозга — правится руками, автогенерации нет осознанно. У проекта-менеджера кредитов ссылка на прод появится, когда у него будет свой домен.
4. ДК и Калинино в проде — переснимать при крупных изменениях.
5. Brain сверяет исполнение D-066 16.09 (нет `obriv.md` в `main`) — ничего делать не нужно, уже так.

## Открытые вопросы для владельца

- Нужен ли на сайте раздел о ценах/условиях работы — сейчас их нет вовсе.
- Порядок карточек: флагманами идут Матрица РМЗ, Гоньба, Сабантуй, Тренер. Меняется перестановкой в `works.ts`.
- Стоит ли исключить собственные визиты из статистики Метрики: сейчас в цифру информера попадают в том числе заходы владельца и проверки из сессий.
