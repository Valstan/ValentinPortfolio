---
description: Старт сессии «Портфолио» — синхра только своего репо (#032) + двухканальный mailbox-check от brain + чтение SESSION_HANDOFF
---

Выполни старт сессии «Портфолио» (ValentinPortfolio) строго по шагам (детали — в `AGENTS.md` §📬 Mailbox check). Порядок жёсткий: **сначала синхронизация только своего репозитория, затем двухканальное read-only чтение mailbox, потом session-память** — pool #032.

1. **Sync свой репо — ПЕРВЫМ:** `git fetch`; если working tree чист и есть отставание — `git checkout main && git pull --ff-only`. Незакоммиченное / не-ff — сообщи, не форсируй.
2. **Brain и соседние репо не синхронизировать:** не выполнять в `../brain_matrica/` и других чужих репозиториях `fetch`, `pull`, `checkout` или любые изменяющие команды. Они могут прямо сейчас использоваться владельцем или другим агентом (мандат 2026-08-04).
3. **Скан входящих из двух read-only источников:**
   - локально прочитать корень `../brain_matrica/mailboxes/ValentinPortfolio/from-brain/*.md`;
   - через GitHub API/веб прочитать `main:mailboxes/ValentinPortfolio/from-brain/*.md` репозитория `brain_matrica`, не клонируя и не синхронизируя его;
   - не читать `DRAFTS/` и `ARCHIVE/`; объединить набор по относительному пути;
   - одинаковые версии читать один раз; если содержимое различается, определить свежесть по истории **этого файла**, а не репозитория: незакоммиченная локальная версия — более свежий кандидат, иначе сравнить последний локальный коммит файла с последним коммитом пути на GitHub;
   - если порядок изменений надёжно не определяется, прочитать обе версии и явно доложить конфликт. Ничего не копировать и не перезаписывать.
4. **Доложи** сводку писем ДО чтения handoff:
   ```
   📬 N писем от brain_matrica:
   - [urgency COMPLIANCE] YYYY-MM-DD-slug — тема
   ```
   `urgency: high` выдели отдельно. Письма без `compliance`: `kind: directive`→MUST, `kind: idea`→SHOULD.
5. **Прочитай** `docs/SESSION_HANDOFF.md` — статус, нитка, следующий шаг. Если `Updated:` старше 14 дней — пометь «может быть неактуально».
6. **Сводка main:** `git log --oneline -5` и `git status`.
7. Кратко предложи следующий шаг из handoff.

Не начинай правки до завершения шагов 1–5.
