# Портфолио (ValentinPortfolio) — Session Handoff

> Sticky-note для непрерывности сессий. Перезаписывается `/close_session`. История — `git log -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE — **прод поднят**
**Updated:** 2026-08-02 (go-live: сайт открывается через интернет, автодеплой работает)
**Branch:** main

## Текущая нитка

Сайт-визитка **валентин.вмалмыже.рф** живёт в проде. Дальше — наполнение и доводка, не постройка.

## Что сделано (2026-08-01 — 2026-08-02)

- **ADR-0011** (PR #1): `AGENTS.md` — канон, `CLAUDE.md`/`GEMINI.md` — тонкие адаптеры, заведён `.gitignore`. Живые ссылки на `CLAUDE.md` переведены; историческая в этом файле не тронута.
- **Скриншоты #079** (PR #2): 7 публичных продов, десктоп + мобайл, счётчики Метрики/LiveInternet резались перехватом до съёмки. Полотна — в `assets/screenshots/` (архив), лёгкие кадры первого экрана — в `web/public/shots/`.
- **Сайт** (PR #4): Next 15 static export, 30 маршрутов. Направление выбрано панелью из 3 дизайн-подходов и 9 судей — победил proof-first.
  - Порядок карточек — **один файл** `web/content/works.ts`.
  - Слой `/zadachi/` — 6 страниц-вопросов заказчика.
  - Правило: **факт без даты замера не публикуется**; каждый помечен «проверяется снаружи» / «замер автора».
  - SEO/GEO #051: JSON-LD граф через `@id`, `/llms.txt`, `/llms-full.txt`, `/facts.json`, robots с явным пропуском ИИ-ботов; `robots.ts`/`sitemap.ts` в корне `app/` (G12).
  - Схемы архитектуры вместо скриншотов там, где экраны публиковать нельзя.
- **Go-live** (2026-08-02): владелец завёл домен и сертификат **на боксе Сетки** (не на Боксе 1, как называл kickoff). Развёрнуто там же; отклонение зафиксировано письмом в brain. TLS — через панель хостинга (G150), на боксе только `listen 80`.
- **Контакты и автодеплой** (PR #6, #7, #8): почта, Telegram, GitHub во всех выходах из одного источника. CI: сборка в Actions (G20), tar-поток (rsync на боксе нет), атомарный симлинк, три последних релиза, смоук **на боксе**.
- Ack'и в brain: ADR-0011, каталог сервисов, прогресс kickoff, **go-live** (PR #3, #5).
- `PORTFOLIO.md` синхронизирован с реестром (Сарафан, Казанская, вМалмыже, Тренер).

## Следующий шаг

1. **Телефон на сайт.** `CONTACTS.phone`/`phoneLabel` в `web/content/site.ts` пусты и не рендерятся. Муниципальный заказчик звонит раньше, чем пишет — заполнить, когда владелец даст номер.
2. **Скриншоты Матрицы РМЗ** — нужен демо-стенд или обезличенные экраны. Пока карточка идёт со схемой архитектуры.
3. **дкмалмыж.рф и калинино-цкс.рф** на 2026-08-01 не отвечали (ERR_TIMED_OUT ×3 при живых остальных). Наблюдение ушло в brain; когда поднимутся — доснять и убрать оговорку с главной.
4. Даты замеров в фактах стареют. Пересматривать при заметных изменениях, не реже раза в квартал.

## Открытые вопросы для владельца

- Рабочий телефон для сайта (см. п. 1).
- Порядок карточек: сейчас флагманами идут Матрица РМЗ, Гоньба, Сабантуй, Тренер. Меняется перестановкой в `works.ts`.
- Оставлять ли сайт на боксе Сетки (там домен и сертификат) или переносить на Бокс 1, как называла kickoff-директива.
