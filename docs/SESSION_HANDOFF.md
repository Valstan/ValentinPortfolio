# Портфолио (ValentinPortfolio) — Session Handoff

> Sticky-note для непрерывности сессий. Перезаписывается `/close_session`. История — `git log -- docs/SESSION_HANDOFF.md`.

**Status:** ACTIVE — прод работает, автодеплой зелёный
**Updated:** 2026-08-02 (витрина наполнена: 12 работ, контакты, автодеплой)
**Branch:** main

## Текущая нитка

Сайт-визитка **валентин.вмалмыже.рф** в проде. Нитка постройки закрыта — дальше наполнение и поддержание в актуальном состоянии.

## Что сделано (2026-08-01 — 2026-08-02)

- **ADR-0011** (PR #1): `AGENTS.md` — канон, `CLAUDE.md`/`GEMINI.md` — адаптеры, заведён `.gitignore`.
- **Скриншоты #079** (PR #2): счётчики Метрики/LiveInternet резались перехватом до съёмки; cookie-плашки скрывались CSS-ом (не нажимались — согласие не давалось). Полотна в `assets/screenshots/`, лёгкие кадры в `web/public/shots/`.
- **Сайт** (PR #4): Next 15 static export, 31 маршрут. Направление выбрано панелью из 3 подходов и 9 судей — победил proof-first.
  - Порядок карточек — **один файл** `web/content/works.ts`; счётчики главной считаются из каталога и не могут разъехаться.
  - Слой `/zadachi/` — 6 страниц-вопросов заказчика.
  - **Факт без даты замера не публикуется**; каждый помечен «проверяется снаружи» / «замер автора».
  - SEO/GEO #051: JSON-LD граф через `@id`, `/llms.txt`, `/llms-full.txt`, `/facts.json`; `robots.ts`/`sitemap.ts` в корне `app/` (G12).
- **Go-live** (2026-08-02): развёрнуто **на боксе Сетки** (не на Боксе 1, как называл kickoff) — владелец завёл туда домен и сертификат. TLS через панель хостинга (G150), на боксе только `listen 80`.
- **Контакты** (PR #6, #7, #10): телефон, почта, Telegram, GitHub — во все выходы из одного источника `web/content/site.ts`.
- **Автодеплой** (PR #6, #8): отдельная пара ключей (#001), tar-поток (rsync на боксе нет), атомарный симлинк, три последних релиза, смоук **на боксе**.
- **Наполнение** (PR #10, #11, #12): Матрица РМЗ с рабочими экранами на обезличенных данных; ДК и Калининская ЦКС на новых доменах; рекламная площадка «Сарафан».
- Ack'и в brain: ADR-0011, каталог сервисов, прогресс kickoff, go-live, поправка по ДК/Калинино (PR #3, #5, #13).

## Грабли этой сессии (уже вылечены, но помнить)

1. **Next 15.5.4 не собирает static export на Windows** — падает на ренейме `500.html`/`pages-manifest.json`. Лечится обновлением до 15.5.22.
2. **При static export заголовок `content-type` из route-handler теряется**: `/llms.txt` отдаёт nginx как `text/plain` **без** `charset`, кириллица превращается в кашу. Лечится только на стороне nginx (`charset utf-8` + `charset_types`). Коварно тем, что `.json` и `.html` при этом выглядят нормально.
3. **Смоук из GitHub Actions до российского хоста ненадёжен** — раннеры стоят вне РФ, `curl (28)` при живом сайте. Смоук перенесён на бокс, внешняя проверка не блокирующая.
4. **Внешний таймаут ≠ «сайт лежит»** — сперва сверь, куда указывает домен (история с ДК и Калинино: приложения работали, домены указывали в пустоту).
5. **fullPage-скриншот не ждёт `loading="lazy"`** — при проверке своего сайта puppeteer'ом надо прокрутить страницу, иначе снимет пустые рамки и решишь, что картинки сломаны.

## Следующий шаг

1. Держать каталог в синхроне с реестром Мозга — правится руками, автогенерации нет осознанно.
2. **Даты замеров стареют.** Числа Сарафана (26 сообществ / 29 173 подписчика) обновляются каждую ночь на его стороне; коммиты Матрицы и Гоньбы — тоже. Пересматривать не реже раза в квартал.
3. Когда ДК и Калининская ЦКС наполнятся контентом — переснять и поднять статус с янтарного до зелёного.
4. Скриншоты остальных закрытых работ (Сарафан-редакция, Карман) — только если появятся обезличенные экраны; сейчас идут со схемами архитектуры.

## Открытые вопросы для владельца

- Оставлять ли сайт на боксе Сетки (там домен и сертификат) или переносить на Бокс 1, как называла kickoff-директива. Мой довод за «оставить»: static export стоит ≈0 RAM, а Бокс 1 у Мозга проходит главным инфра-риском портфеля.
- Порядок карточек: флагманами идут Матрица РМЗ, Гоньба, Сабантуй, Тренер. Меняется перестановкой в `works.ts`.
- Нужен ли на сайте раздел о ценах/условиях работы — сейчас их нет вовсе.
