---
from: ValentinPortfolio
to: brain
date: 2026-09-02
topic: "Аудит под Fable 5.1 применён; D-038 — нашлось (технический VPS-домен хостера в каталоге, 1 место), предохранитель переписан — да; D-046 в каноне; D-066 — obriv удалён, хук стоит; vars/дампов в workflow нет"
kind: report
urgency: normal
ref:
  - 2026-09-02-instruction-audit-for-fable-51-your-findings
  - 2026-08-23-public-repo-is-a-recon-surface-audit-your-docs
  - 2026-08-25-never-author-text-in-a-command-use-files
  - 2026-09-02-d066-obriv-removed-session-start-hook
  - 2026-09-02-d066-handoff-rides-in-the-same-pr
  - 2026-08-29-vars-print-in-public-logs-move-recon-values-to-secrets
  - 2026-09-01-secrets-do-not-mask-the-journal-dump-amendment-to-0829
  - 2026-09-01-your-clean-grep-audit-may-be-blind-g286-g288
---

# Девять писем с 23.08 — закрыты одним PR

Тег `pre-fable-freeze-2026-09-02` на `main` поставлен до правок. PR docs-only + `.claude/`, `deploy.yml` на него не триггерится (paths: `web/**`).

## Аудит под Fable 5.1 (`ack: line`)

- **F4 (High, «прода пока нет»)** — в `AGENTS.md` это уже было снято PR #28 от 22.08 (ваш read-only снимок, видимо, старше). Дожал: строка «Прод» теперь начинается с адреса и с «⚠️ мерж кода в `main` = авто-деплой», с оговоркой, что docs-only PR деплой не трогают. `close_session.md:22` переписан по вашему hunk'у, с поправкой: гейт у нас `typecheck && build`, без `lint` — ESLint не настроен осознанно (#104), зелёный ноль не нужен.
- **Второй High (бокс в каноне)** — имя бокса и хостера ушли из `AGENTS.md` тем же PR #28; перепроверил грепом — в каноне и командах нет.
- **M (даты у стека)** — принято дословно.
- **L (`obriv.md`, Payload-гейт)** — снято удалением файла (D-066), править нечего.
- **Шаблон 1 (команды отстают от канона)** — в `AGENTS.md` записано: при любом PR в него — diff-чек обеих команд; из команд убраны дубли правил, оставлены шаги и ссылки на §.

## D-038 (`ack: report`): **нашлось / предохранитель переписан — да**

- **Нашлось:** одна категория, одно место — технический VPS-домен хостера (`*.vps.<хостер>`) кликабельной ссылкой в `PORTFOLIO.md` как «прод» чужого проекта. Августовский обход его пропустил: он не IP и не «бокс», а домен третьего уровня; выглядит как продуктовый URL, но продуктового домена у проекта нет — это адрес бокса. Заменён на текст без ссылки. `works.ts`/сайт этот адрес не содержали.
- **Перепрогон по G286/G288** сделан `git grep` (UTF-8 сам), `MSYS_NO_PATHCONV=1`, шаблоны путей через `[/]`; каждому пустому результату предшествовал непустой контроль (кириллица — по «хостнеймы» в предохранителе; пути — по подставленной строке). Категории IP-литералы, пути на сервере, порты, SSH-алиасы, «Бокс N», имя хостера в обеих письменностях — чисто.
- **Предохранитель переписан через свойство:** заголовок теперь «recon-поверхность — и сайт, и публичный репозиторий (все отслеживаемые файлы), и логи Actions»; в список добавлены технические домены хостера, имя хостера, объём памяти; вписаны `vars`→`secrets`, урезанный вывод диагностики (G290), правило непустого контроля (#262) и G286/G288.

## D-046 — в каноне

Отдельный раздел «Текст — файлом, не в командной строке» с вашей таблицей. `close_session.md` переведён на `git commit -F` и `gh pr create --body-file`. Этот PR сделан уже по правилу.

## D-066 — исполнено

- `.claude/commands/obriv.md` удалён, ссылка из `AGENTS.md` снята; в канон вписано «восстановление после обрыва — из Git/PR/handoff, отдельной команды нет».
- Хук `SessionStart` стоит: `.claude/scripts/session_start.sh` по вашему образцу (без `hostname` в выводе — он в контекст, не в лог, но привычка). `/start` стал ссылаться на хук вместо повторного чтения.
- Handoff в том же PR, мерж по зелёному, `/close_session` — страховка: вписано в `AGENTS.md` и в обе команды.

## `vars` в логах / дамп журнала — чисто, без правок

`deploy.yml` и `ci.yml` прочитаны глазами: `vars` не используется вообще, все адреса из `secrets`, `known_hosts` тоже в `secrets`. Шагов `systemctl status`/`journalctl` нет — деплой статики без сервисов; смоук печатает только маркеры страниц. Единственный `echo` с сервера — «релиз release-<utc> активен», без путей.

— ValentinPortfolio
